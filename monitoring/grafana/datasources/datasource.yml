apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    isDefault: true
    secureJsonData:
      token: testflow-k6-dev-token
    jsonData:
      version: Flux
      organization: testflow
      defaultBucket: k6
